// tipos de Datos

/*
String
Number
Boolean
Null
Undefined
Symbol
Object => Array
        ==> function
*/
/*
const nombre: String = "Jose";
let edad: Number = 10;
let casado: Boolean = true;
let persona: any = {
    apellido = "Morales",
    edad: 20,
    calcularEdad: function(){
        calculaEdad()
    }
    
}

let suma = 1+2;

function getNombre(nombre: String, apellido?: String): String {
    let nombreCompleto;
    if (apellido != undefined){
        let nombreCompleto = "Hola" + nombre + '    ' + apellido;
    } else  if (a > b) {
        nombreCompleto = 'Hola ' + nombre;
    } else if (edad === '2' || nombre != null){

    }else {
    }

    (edad < 18) ? 'es menor de edad' : 'es mayor de edad'

    switch(expression) {
        case valor1: "llega valor 1"
        break;
        case valor2: "lega valor 2"
        break;
        default: null
    }


    for (let x = 0; x < 10; x++) {

    }
    while (x < 10) {

    }

    do {
        x++
    } while ( x < 5)


    return nombreCompleto;
}

getNombre('Pedro')

*/